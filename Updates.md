# Updates

## 2026-09-08 — 拼写失败立即清空并支持空 Enter

- 拼写错误后立即清空输入框，同时保留正确单词提示，让用户从头重拼；再次正确输入只用于解锁前进，不覆盖首次错误记录。
- 拼写首个输入为空时可直接按 Enter，按本轮“不认识”计分并显示与拼错相同的原单词提示；自动发音仍在这次提交中播放。
- 空输入的首次提交按钮明确显示“不会拼，查看答案”，错误后的空输入则等待用户重新键入。

## 2026-09-08 — 辨识发音改为“进入、展开、下一词”节奏

- 进入每个辨识单词的第一页时自动发音一次；首次按 Enter“认识”或 N“不认识”后展开答案，并第二次朗读当前词。
- 第一页选择“不认识”后，答案页只保留 Enter“下一个”，N 不再响应；选择“认识”后才显示 N“记错了，下一词”。
- Enter 前进或从“认识”状态用 N 改判前进时，下一词出现并自动发音；同步更新按钮文案、快捷键帮助和回归测试。
- 首屏“认识 / 不认识”改为完全同级的中性按钮：宽度、填充、边框、阴影与键帽权重一致，避免视觉上默认诱导 Enter；答案页的主次层级保持不变。

## 2026-09-07 — 修复发音静音与按钮焦点卡住

- 回看 `v2-latest` 后确认 Arc 兼容的关键不是额外恢复语音引擎，而是必须在用户按键或点击的同一个事件内调用 Web Speech；v3 原先在 React 状态更新后的 effect 中播放下一词，会错过 Arc 接受的触发时机。
- 发音 hook 恢复为 v2 已验证的队列行为：即时播放先取消旧队列，答错时先即时重播当前词、再排队下一词；揭示、推进和拼写提交都直接在对应操作中发音，不再由状态变更 effect 补播。结合对比视频中 Arc 全程无音频状态、Chrome 正常输出的证据，播放中的 utterance 现在会保留到结束或报错，避免 Arc 在同步调用返回后过早回收。
- 鼠标点击按钮、链接、开关后统一释放焦点，下一次 Enter、N、Space 等立即回到学习快捷键；用户主动按 Tab 时仍保留标准键盘焦点和可见提示。
- 用 Esc 关闭 Import/更多面板后不再把焦点送回原按钮，下一次 Enter 会直接继续翻面或前进。
- 新增回归测试，覆盖页面加载不抢跑发音、揭示与推进的同步播放顺序，点击发音和词源按钮后继续用 Enter 推进，以及 Import / 更多经 Esc 关闭后继续用 Enter 学习。
- 真实 Chrome 检测到 180 个本机语音；点击发音后语音引擎进入 `speaking` 状态且未暂停。点击发音与词源按钮后活动焦点均回到页面主体，Enter 可继续翻面和进入拼写。
- 生产构建的真实浏览器时机检查确认：揭示当前词与推进到下一词的两次 `speak()` 调用中，`navigator.userActivation.isActive` 都为 `true`，与 v2 在 Arc 上可工作的触发方式一致。
- 控制台截图中的 `content.js`、`notKey` 和 `Control` 均不在项目源码中，判定为浏览器扩展脚本错误；应用修复不依赖该扩展。

## 2026-09-07 — 闪词 3.0 从零重写

- 在独立分支 `codex/flashvocab-3-rewrite` 和独立 worktree 中完成单页重写；原工作区及其中的用户改动保持原样，未 push、未部署。
- 首屏改为 Import 优先的词表准备页，学习流程固定为“辨识 → 拼写 → 辨识”；移除旧休息页、熟悉池、跨模式绑定、返场比例上限和可选复杂模式。
- 辨识与拼写各自采用“两轮正确、完整休息一轮、随后返场”的极简规则；补齐回看不重复计分、拼错必须改正、暂停恢复、1–2 词与全休息边界。
- 保留现有多格式导入和内置 24 词，导出不丢未知字段或额外例句；新增 JSON / Markdown 导出、移出与找回、单槽 Undo，以及只保留词表资产的新本地存储层。
- 新界面采用安静阅读布局、英文释义优先、独立中文校准、按用法切换例句、折叠词源和右侧管理面板；键盘学习时外围控件会淡出，鼠标或 Tab 可随时唤回。
- 构建配置显式去重 React 与 React DOM，避免独立 worktree 复用依赖目录时把两份 React 打进生产文件而造成白屏。
- 自动检查通过 35 个纯逻辑测试、18 个 UI 行为测试、ESLint 与生产构建；真实 Chrome 完成 2 词 JSON 文件导入、24 词辨识和 24 词拼写，并验证刷新重置学习、切换标签保留现场、窄窗、200% 缩放、静默模式、复制反馈、发音状态与六个关键视觉状态。Arc 诊断确认可读取 180 个系统语音，但无用户操作的自动调用会被浏览器以 `not-allowed` 拒绝；应用保留直接操作触发的播放并在无 Web Speech API 时安全降级。

## 2026-09-01 — 整合 User Flow 决策批注

- 将 D2–D8 从建议方案更新为已确认规则；D3 的两轮辨识门槛与 D4 的 3–5 个拼写小批次标记为实验性，等待下一份真实作业验证。
- 新词表改为解析成功后直接替换，并以事后 Undo 代替事前确认；当前无待办时，`Enter` 结束本次并停留在完成态。
- 补全拼写尾批规则：仍可积累时不因 1–2 个候选切换阶段，已无法继续积累时放行尾批，避免流程卡住。
- D1 暂不凭文字决定，留到低保真阶段对比两种首次首屏；决策总表的每个编号均增加文档内跳转链接。
- 将“汇总表编号必须链接到正文对应位置”记录为项目文档规范；本次只更新文字逻辑，未修改 FigJam、界面或代码。

## 2026-08-30 — 闪词 3.0 完整 User Flow

- 将 3.0 从打开网页到再次使用的完整路径整理为独立文档，覆盖准备或恢复词表、逐词辨识、轮末四种分流、合格词拼写、返场、完成、移除、重置和异常恢复。
- 所有关键选择统一用“状态 → 条件 → 分支 → 结果”表达，并区分已对齐原则、当前推荐方案和需要产品负责人确认的 8 项决策。
- 建立可编辑 FigJam，包含总体流程，以及准备与恢复、辨识、轮末自动分流、拼写回流、帮助与异常五张分图。
- 保存用户提供的两张产品逻辑核心图作为 User Flow 的上位依据；本次仍不涉及应用代码或已上线行为。

## 2026-08-30 — 闪词 3.0 重构设计底稿

- 完成 3.0 设计准备文档，将产品收敛为“面向临近课堂词汇 quiz 的键盘优先个人词表冲刺网页”，明确目标用户、核心场景、非目标和成功信号。
- 建议把辨识证据纳入默认主循环，由系统在轮末自动选择“继续辨识”或“合格词拼写”，同时保留用户次级改选；熟悉返场降为后台策略。
- 重组功能层级：学习主层只保留当前任务，上下文层承载例句、词源和深度理解，导入导出、设置、熟悉池与重置进入工具层。
- 记录 9 类关键状态、阅读动线、静默模式、键帽和图形化进度原则，以及空返场、导入替换、移除与重置等边界状态。
- 补充释义层级方向：当前优先把英英释义简化到可直接理解，中文降为最后的定位与校准层；未来单独试验用 AI 寻找或生成的核心画面作为第一释义信息，不阻塞 3.0 初始设计。
- 保存本次实际流程截图与用户标注作为 2.5 现状证据；本次未修改应用代码或已上线行为。

## 2026-08-29 — Figma 交互原型

- 将当前界面基线扩展为 13 个画板的可播放原型，并将刷词翻面、前后切换、熟悉模式评分、休息屏、拼写对错结果、熟悉词池、设置开关、Guidebook、Reset / Undo 和 GitHub 按已上线行为连接。
- `FlashVocab 2 · Start` 以默认的隐藏单词卡作为原型入口；核心流支持鼠标点击与 Enter、Space、N、Esc、Tab / ← 键。
- Figma 无法执行浏览器文件选择、下载、剪贴板和自由文本拼写判断，因此这些浏览器原生动作保留为界面与结果状态，不伪造不一致的交互。

## 2026-08-29 — Figma 当前界面基线

- 在 `FlashVocab 2` 文件的 `FlashVocab 2 — Current UI Baseline` Section 建立了 10 个可编辑桌面画板，完整记录当前学习、休息、拼写、熟悉词池、Guidebook 和空词库状态。
- 画板复用当前的沙色、珊瑚、青绿色视觉语言与可开关学习功能，用于后续 UI/UX 升级前后对比。
- Figma 未提供原界面的 Avenir Next / Iowan Old Style；基线以 Libre Baskerville 作为标题近似字体、Inter 作为正文近似字体。

## 2026-08-29 — 辨识下一词自动发音

- 辨识完成并进入下一张时，自动朗读新出现的单词，无需再次翻面。
- 答错时会先重播当前词，再保留下一词的播放队列；关闭 `自动美式发音` 后两种自动播放都会停止。
- 新增辨识推进和关闭自动发音时的回归测试。

## 2026-08-29 — 取消按钮焦点提示

- 页面按钮、单词复制入口和 GitHub 入口不再进入键盘 Tab 焦点序列；点击操作会立即释放焦点。
- 移除应用内焦点描边和开关焦点提示，避免 Chrome 默认蓝色高亮框。
- 点击 Import 后焦点不会停在 Import 按钮，紧接着按 Enter 将继续刷词，不会重复打开导入选择器。

## 2026-08-27 — 浏览器美式发音

- 使用 Web Speech API 朗读原始 `term`，无需音频文件、外部服务或 API Key；优先选择设备上的本地 `en-US` voice。
- 辨识答案从隐藏变为显示时自动播放；辨识选择「没想起来」时再播放一次；拼写正确或错误的首次提交都会播放一次。
- 音标区和拼写正确答案旁新增带无障碍名称、键盘 focus 和 SVG 图标的重播按钮，点击不会误触词卡翻面。
- 新增默认开启、独立持久化的 `自动美式发音` 开关；关闭后保留手动重播，Reset Deck 不改变该偏好。
- 每次播放前清除旧语音队列；API 或 voice 不可用时安全降级，不影响学习流程。
- 新增 voice 选择、辨识揭示与答错触发、拼写对错触发、关闭自动播放、手动重播和 API 缺失回归测试。

## 2026-08-27 — 两轮熟悉与同步返场

- 熟悉阈值从连续正确 4 轮缩短为 2 轮，辨识与拼写的状态数字和开关文案同步更新。
- 辨识已隐藏时，拼写首次正确即可同步隐藏；若拼写先正确 1 次，辨识随后达到隐藏条件，也会立即完成绑定。
- 绑定词以辨识实际返场为准进入拼写返场，辨识未选中的到期词不会被拼写提前带回；同步候选在 25% 上限内优先。
- 辨识返场正确会同步下一次间隔，辨识返场错误会解除两边隐藏；拼写返场错误只解除拼写侧绑定。
- 新增纯算法与 UI 回归测试，覆盖两轮入池、双边绑定、辨识主导返场及失败释放。

## 2026-08-27 — 可选复杂模式

- 底部新增两个默认关闭、独立持久化的开关：`词源与对应概念` 和熟悉返场。
- 简约模式不显示词源区、熟悉状态或熟悉池，所有未删除单词保持普通循环。
- 「想起来了 / 没想起来」按钮和 `N` 快捷键跟随熟悉返场开关；关闭时翻面后只需 Enter 进入下一张。
- 关闭熟悉机制时仍能正常完成轮次，但不会改变 streak、隐藏、返场或 lapse；已有复杂模式进度保留，关闭瞬间会把隐藏词补回当前队列。
- 本节记录的是当时的四轮规则；当前规则见上方「两轮熟悉与同步返场」。
- 自动化测试更新为 35 个逻辑测试和 10 个 UI 行为测试，覆盖默认值、独立持久化、简约轮次与隐藏词恢复。

## 2026-08-27 — 内置词源字段

- 内置 24 词全部补全 `wordOrigin`（按拉丁、希腊、古法语来源拆解）与 `relatedWord`（B1-B2 级反义词或对应概念）。
- 留了几处卡片间呼应：Vacant↔vacate、Translucent↔Lucid、Negligence↔Diligence，复习时可以串起来。
- `vocab.md` 同步补全 `Word Origin` 与 `Related Word` 行；Markdown 导入解析器原生认识这两个字段，往返一致。

## 2026-08-27 — Codebase refactor

- 将内置词库、导入导出、浏览器存储和键盘行为从 `App.jsx` 拆为独立模块，删除应用内的重复实现；主组件改为状态协调和视图组合。
- study 与 spell 队列统一保存稳定 card ID，移除依赖 JavaScript 对象引用相等的状态逻辑；删除、Undo、轮次恢复和导入后的队列处理使用同一身份模型。
- localStorage 增加版本、结构归一化、无效 ID 清理、索引收敛和读写异常保护；损坏快照不再导致应用启动失败。
- 导入层统一支持 JSON、Markdown、纯文本、CSV 和 DOCX，保留自定义字段，并补全多条例句的 Markdown 往返导出；旧版 `.doc` 会给出明确提示。
- study、rest、spell、底部控制栏、Guidebook 和熟悉池拆为独立组件，例句高亮提取为可测试纯函数。
- 新增 ESLint、Vitest/jsdom、Testing Library 和 GitHub Actions CI；现有测试扩充为 32 个逻辑测试与 7 个 UI 行为测试。
- Vite 升级到 8.2.2，生产构建重新生成；依赖审计无已知漏洞。
- `ui-ux-pro-max` skill 已全局安装到 `.agents` 并同步到 `.hermes`；项目内旧 `.cursor`、`.shared` 与 `.codex` 文件已经清除，ESLint 也不再为这些目录保留特殊忽略规则。

## Earlier updates

轮次熟悉池

辨识和拼写现在分别记录连续正确次数；同一单词、同一 mode、同一轮最多计分一次
某个 mode 连续正确 4 轮后，从下一轮隐藏，并随机完整间隔 1-2 轮
到期词按逾期轮数优先、同级随机的方式分批返场；普通词存在时，返场词最多约占最终队列的 25%
返场正确会重新安排 1-2 轮间隔；返场错误只重置当前 mode，另一个 mode 不受影响
辨识模式翻开后新增「想起来了 / 没想起来」明确自评，Enter 记正确，N 记错误；单纯翻面不计分
拼写模式以第一次提交作为本轮成绩，看到答案后的改正不会覆盖错误
新增可展开的「暂时熟悉池」，分别显示辨识和拼写状态及最早返场轮次
学习记录、轮次、当前队列、导入词库、删除及最后一次 Undo 状态使用 localStorage 保存，刷新后继续
熟悉词仍参与 JSON/Markdown 导出；Remove 继续表示从词库和导出中删除
新增 learningAlgorithm 纯函数模块和 10 项自动化测试，覆盖稳定 ID、四次入池、轮次间隔、模式隔离、跳卡轮次保护、小队列防饥饿和逾期优先级

复制、保序导出与词源提示词

点击卡片中的单词标题会复制原始拼写，答案隐藏或显示时都可用，且不会触发卡片翻面
复制后立即释放单词标题焦点，不显示持续的卡片焦点框，也不阻碍后续快捷键
顶部新增 Export JSON，按原始导入顺序导出本轮未删除的单词
JSON 导入会保留未知自定义字段，删除后的导出仍带完整信息
词卡标准字段新增 wordOrigin 和 relatedWord，翻开后显示词根词源与成对的相反词或对应概念
Guidebook 的批量 JSON 提示词已要求生成上述字段
顶部和 Guidebook 新增「词根·感觉·画面」提示词复制入口，内容来自 src/prompts/deep-understanding.md
手动删除最后一个单词后显示 Congratulations 完成状态，并保留 Undo 与 Reset
从休息或拼写界面点击 Remove 会先回到刷词模式，避免删除后停留在无效进度
全局刷词快捷键会忽略按钮和输入控件，键盘可正常触发顶部操作

例句卡面更新

study 模式翻开卡片后，不再显示 phrases 列表，改为显示 2-3 条轻量例句列表
移除了例句外层强调框，只保留更小字号的句子列表
每个词条主字段改为 `examples`，每项是 `{ sentence, focus }`
卡面会优先加粗每条例句里的 `focus`；若缺失或句中找不到，再回退到加粗 `term`
导入提示词、JSON 归一化、JSON/Markdown 导出都已切到 `examples`
`vocab.md` 词条格式从单组 Sentence/Focus 扩展为 2-3 组编号 Sentence/Focus
整套内置例句已改写为更偏 B1-B2 的日常语境，降低法律/新闻/政治类表达的比例

快速启动脚本

根目录新增 `run.sh`
脚本会先切到项目目录；若缺少 `node_modules`，会自动执行 `npm install`
随后用 `npm run dev -- --host 0.0.0.0` 启动本地开发服务器

贫血模式

在最后一张词（N/N）已 revealed 的情况下按 Enter → 进入贫血屏，只有「随手拼？」标题和操作说明
贫血屏：Enter → 回到第 1 张继续刷词，Space → 进入随手拼
随手拼

只显示词性 + 中英文意思，单词隐藏，光标闪烁
底部总进度条和计数会随模式切换：刷词模式跟随 `index`，随手拼模式跟随 `spellIndex`
实时显示键入内容（任意字母键追加）
Enter 提交：正确 → 绿色 + 显示带音节点的正确拼写；错误 → 红色 shake + 显示正确答案
拼错后直接再按 Enter → 再次 shake；开始键入字母 → 自动清空重拼
Backspace 逐字删除
拼完所有词 → 自动回普通刷词
Esc → 随时退出随手拼
shake 动画：用 key={shakeKey} 每次递增让 React 重新挂载元素，CSS animation 自动从头播，不用 setTimeout。

循环洗牌

每轮从头开始时默认自动打乱词序（Fisher-Yates），新首卡不会是刚看完的那张
触发点：Next 末卡绕回、贫血屏 Enter 回刷词、拼完所有词自动回刷词
底部控制栏新增 Shuffle Loop: On / Off 按钮，关闭后循环保持当前顺序
Reset Deck 恢复原始顺序，不立即洗
随手拼沿用当前词序，不在拼写中途打乱
